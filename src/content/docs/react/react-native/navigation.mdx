---
title: Navigation
description: Master React Navigation v6/v7 with stack, tab, drawer, and nested navigators. Learn screen params, deep linking, authentication flows, and navigation hooks.
sidebar:
  order: 4
tags: [react, mobile]
---

import { Steps, Aside, Tabs, TabItem, Card, CardGrid } from '@astrojs/starlight/components';

React Navigation is the standard navigation library for React Native. It provides stack, tab, drawer, and material navigators with deep linking, authentication flows, and a powerful hooks API.

## Installation

```bash
npx expo install @react-navigation/native
npx expo install react-native-screens react-native-safe-area-context

# Stack Navigator
npx expo install @react-navigation/native-stack

# Tab Navigator
npx expo install @react-navigation/bottom-tabs

# Drawer Navigator
npx expo install @react-navigation/drawer react-native-gesture-handler react-native-reanimated
```

For bare React Native, run `pod install` after installing iOS dependencies.

## NavigationContainer

Wrap your root component with `NavigationContainer`. It manages navigation state and provides context to all child navigators.

```tsx
// App.tsx
import { NavigationContainer } from '@react-navigation/native';
import { RootNavigator } from './src/navigation/RootNavigator';

export default function App() {
  return (
    <NavigationContainer>
      <RootNavigator />
    </NavigationContainer>
  );
}
```

## Type-Safe Navigation

Define your param types before creating navigators for full TypeScript support:

```typescript
// src/navigation/types.ts
import { NativeStackScreenProps } from '@react-navigation/native-stack';
import { BottomTabScreenProps } from '@react-navigation/bottom-tabs';

// Root stack param list
export type RootStackParamList = {
  Home: undefined;
  PostDetail: { postId: string; title?: string };
  UserProfile: { userId: string };
  Settings: undefined;
  NotFound: undefined;
};

// Tab param list
export type TabParamList = {
  Feed: undefined;
  Search: { initialQuery?: string };
  Notifications: undefined;
  Profile: { userId: string };
};

// Typed screen props
export type PostDetailScreenProps = NativeStackScreenProps<
  RootStackParamList,
  'PostDetail'
>;

// Global type augmentation for useNavigation()
declare global {
  namespace ReactNavigation {
    interface RootParamList extends RootStackParamList {}
  }
}
```

## Stack Navigator

The `createNativeStackNavigator` uses native UINavigationController on iOS and Fragments on Android — giving you native transitions and gestures.

```tsx
// src/navigation/RootNavigator.tsx
import { createNativeStackNavigator } from '@react-navigation/native-stack';
import { HomeScreen } from '../screens/HomeScreen';
import { PostDetailScreen } from '../screens/PostDetailScreen';
import { SettingsScreen } from '../screens/SettingsScreen';
import type { RootStackParamList } from './types';

const Stack = createNativeStackNavigator<RootStackParamList>();

export function RootNavigator() {
  return (
    <Stack.Navigator
      initialRouteName="Home"
      screenOptions={{
        headerStyle: { backgroundColor: '#fff' },
        headerTintColor: '#007AFF',
        headerTitleStyle: { fontWeight: '600' },
        headerBackTitleVisible: false,  // iOS: hide back title
        animation: 'slide_from_right',
        contentStyle: { backgroundColor: '#f9f9f9' },
      }}
    >
      <Stack.Screen
        name="Home"
        component={HomeScreen}
        options={{ title: 'Feed', headerShown: true }}
      />
      <Stack.Screen
        name="PostDetail"
        component={PostDetailScreen}
        options={({ route }) => ({
          title: route.params.title ?? 'Post',
          headerLargeTitle: true,      // iOS large title on scroll
        })}
      />
      <Stack.Screen
        name="Settings"
        component={SettingsScreen}
        options={{
          presentation: 'modal',       // iOS: card-style modal
          animation: 'slide_from_bottom',
          headerShown: false,
        }}
      />
    </Stack.Navigator>
  );
}
```

### Stack Animation Options

```typescript
// Available animations for native stack:
type StackAnimation =
  | 'default'           // Platform default
  | 'fade'
  | 'fade_from_bottom'
  | 'flip'
  | 'none'
  | 'simple_push'
  | 'slide_from_bottom'
  | 'slide_from_right'
  | 'slide_from_left'
  | 'ios'               // iOS-style slide
```

## Tab Navigator

```tsx
// src/navigation/TabNavigator.tsx
import { createBottomTabNavigator } from '@react-navigation/bottom-tabs';
import { Ionicons } from '@expo/vector-icons';
import { FeedScreen } from '../screens/FeedScreen';
import { SearchScreen } from '../screens/SearchScreen';
import { NotificationsScreen } from '../screens/NotificationsScreen';
import { ProfileScreen } from '../screens/ProfileScreen';
import type { TabParamList } from './types';

const Tab = createBottomTabNavigator<TabParamList>();

export function TabNavigator() {
  const unreadCount = useNotificationCount(); // your hook

  return (
    <Tab.Navigator
      screenOptions={({ route }) => ({
        tabBarIcon: ({ focused, color, size }) => {
          const icons: Record<keyof TabParamList, [string, string]> = {
            Feed: ['home', 'home-outline'],
            Search: ['search', 'search-outline'],
            Notifications: ['notifications', 'notifications-outline'],
            Profile: ['person', 'person-outline'],
          };
          const [filled, outline] = icons[route.name];
          return (
            <Ionicons
              name={(focused ? filled : outline) as any}
              size={size}
              color={color}
            />
          );
        },
        tabBarActiveTintColor: '#007AFF',
        tabBarInactiveTintColor: '#8E8E93',
        tabBarStyle: {
          backgroundColor: '#fff',
          borderTopColor: '#eee',
        },
        headerShown: false,
      })}
    >
      <Tab.Screen name="Feed" component={FeedScreen} options={{ tabBarLabel: 'Home' }} />
      <Tab.Screen name="Search" component={SearchScreen} />
      <Tab.Screen
        name="Notifications"
        component={NotificationsScreen}
        options={{
          tabBarBadge: unreadCount > 0 ? unreadCount : undefined,
          tabBarBadgeStyle: { backgroundColor: '#FF3B30' },
        }}
      />
      <Tab.Screen
        name="Profile"
        component={ProfileScreen}
        initialParams={{ userId: 'me' }}
      />
    </Tab.Navigator>
  );
}
```

## Drawer Navigator

```tsx
// src/navigation/DrawerNavigator.tsx
import { createDrawerNavigator } from '@react-navigation/drawer';
import { View, Text, Pressable, StyleSheet } from 'react-native';
import { DrawerContentScrollView, DrawerItemList } from '@react-navigation/drawer';

const Drawer = createDrawerNavigator();

function CustomDrawerContent(props: any) {
  return (
    <DrawerContentScrollView {...props}>
      <View style={styles.drawerHeader}>
        <Text style={styles.drawerUserName}>John Doe</Text>
        <Text style={styles.drawerUserEmail}>john@example.com</Text>
      </View>
      <DrawerItemList {...props} />
      <Pressable style={styles.logoutBtn} onPress={() => signOut()}>
        <Text style={styles.logoutText}>Sign Out</Text>
      </Pressable>
    </DrawerContentScrollView>
  );
}

export function DrawerNavigator() {
  return (
    <Drawer.Navigator
      drawerContent={props => <CustomDrawerContent {...props} />}
      screenOptions={{
        drawerType: 'slide',
        drawerStyle: { backgroundColor: '#fff', width: 280 },
        overlayColor: 'rgba(0,0,0,0.4)',
        drawerActiveTintColor: '#007AFF',
        drawerInactiveTintColor: '#333',
      }}
    >
      <Drawer.Screen name="Home" component={HomeScreen} />
      <Drawer.Screen name="Settings" component={SettingsScreen} />
    </Drawer.Navigator>
  );
}

const styles = StyleSheet.create({
  drawerHeader: { padding: 24, borderBottomWidth: 1, borderBottomColor: '#eee', marginBottom: 8 },
  drawerUserName: { fontSize: 18, fontWeight: 'bold' },
  drawerUserEmail: { fontSize: 14, color: '#666', marginTop: 4 },
  logoutBtn: { margin: 16, padding: 12, borderRadius: 8, backgroundColor: '#FFF0F0' },
  logoutText: { color: '#FF3B30', fontWeight: '600', textAlign: 'center' },
});
```

## Nested Navigators

Real apps combine multiple navigator types. A common pattern is a root stack that contains a tab navigator:

```tsx
// src/navigation/RootNavigator.tsx
const RootStack = createNativeStackNavigator<RootStackParamList>();

export function RootNavigator() {
  const { isAuthenticated } = useAuth();

  return (
    <RootStack.Navigator screenOptions={{ headerShown: false }}>
      {isAuthenticated ? (
        // Authenticated screens
        <>
          <RootStack.Screen name="Tabs" component={TabNavigator} />
          <RootStack.Screen
            name="PostDetail"
            component={PostDetailScreen}
            options={{ headerShown: true }}
          />
          <RootStack.Screen
            name="Settings"
            component={SettingsScreen}
            options={{ presentation: 'modal', headerShown: true }}
          />
        </>
      ) : (
        // Auth screens
        <>
          <RootStack.Screen name="Login" component={LoginScreen} />
          <RootStack.Screen name="Register" component={RegisterScreen} />
          <RootStack.Screen name="ForgotPassword" component={ForgotPasswordScreen} />
        </>
      )}
    </RootStack.Navigator>
  );
}
```

:::tip[Conditional Navigation for Auth]
React Navigation recommends using conditional rendering rather than navigation.navigate() to switch between auth and app screens. When `isAuthenticated` changes, the navigator automatically shows the correct screens.
:::

## Passing Params to Screens

```tsx
// Navigate with params
function PostListItem({ post }: { post: Post }) {
  const navigation = useNavigation<NavigationProp<RootStackParamList>>();

  return (
    <Pressable
      onPress={() =>
        navigation.navigate('PostDetail', {
          postId: post.id,
          title: post.title,
        })
      }
    >
      <Text>{post.title}</Text>
    </Pressable>
  );
}

// Receive params in screen
function PostDetailScreen({ route, navigation }: PostDetailScreenProps) {
  const { postId, title } = route.params;

  // Update params (e.g., after loading data)
  React.useLayoutEffect(() => {
    navigation.setOptions({ title: title ?? 'Post' });
  }, [navigation, title]);

  return <PostContent id={postId} />;
}
```

## Navigation Hooks

### useNavigation

```tsx
import { useNavigation } from '@react-navigation/native';
import { NativeStackNavigationProp } from '@react-navigation/native-stack';
import type { RootStackParamList } from '../navigation/types';

type NavProp = NativeStackNavigationProp<RootStackParamList>;

export function BackButton() {
  const navigation = useNavigation<NavProp>();

  return (
    <Pressable onPress={() => navigation.goBack()}>
      <Text>← Back</Text>
    </Pressable>
  );
}
```

### useRoute

```tsx
import { useRoute } from '@react-navigation/native';
import type { RouteProp } from '@react-navigation/native';
import type { RootStackParamList } from '../navigation/types';

export function PostTitle() {
  const route = useRoute<RouteProp<RootStackParamList, 'PostDetail'>>();
  return <Text>{route.params.title}</Text>;
}
```

## Navigation Methods

```typescript
// Navigate to a screen (won't go if already there)
navigation.navigate('PostDetail', { postId: '123' });

// Push a new instance (even if already in stack)
navigation.push('PostDetail', { postId: '456' });

// Replace current screen
navigation.replace('Login');

// Go back one screen
navigation.goBack();

// Go back to specific screen
navigation.navigate('Home');

// Go back to the first screen in stack
navigation.popToTop();

// Reset navigation state completely
navigation.reset({
  index: 0,
  routes: [{ name: 'Home' }],
});

// Navigate inside a nested navigator
navigation.navigate('Tabs', {
  screen: 'Profile',
  params: { userId: '123' },
});
```

## Deep Linking

```typescript
// app.json — configure scheme
{
  "expo": {
    "scheme": "myapp",
    "web": { "bundler": "metro" }
  }
}
```

```tsx
// Navigation container with deep linking
const linking = {
  prefixes: ['myapp://', 'https://myapp.com'],
  config: {
    screens: {
      Home: '',
      PostDetail: 'posts/:postId',
      UserProfile: 'users/:userId',
      Settings: 'settings',
      Tabs: {
        screens: {
          Feed: 'feed',
          Search: 'search',
          Profile: 'profile',
        },
      },
    },
  },
};

export default function App() {
  return (
    <NavigationContainer linking={linking} fallback={<LoadingScreen />}>
      <RootNavigator />
    </NavigationContainer>
  );
}
```

```bash
# Test deep links from terminal:
# iOS Simulator
xcrun simctl openurl booted "myapp://posts/123"

# Android Emulator
adb shell am start -W -a android.intent.action.VIEW -d "myapp://posts/123" com.myapp
```

## Linking Module

```typescript
import { Linking } from 'react-native';

// Open a URL in the default browser / app
async function openExternalLink(url: string) {
  const canOpen = await Linking.canOpenURL(url);
  if (canOpen) {
    await Linking.openURL(url);
  } else {
    console.warn(`Cannot open URL: ${url}`);
  }
}

// Open app settings
await Linking.openSettings();

// Listen for incoming deep links when app is already open
useEffect(() => {
  const subscription = Linking.addEventListener('url', ({ url }) => {
    console.log('Incoming deep link:', url);
  });

  // Handle initial URL (app launched from a link)
  Linking.getInitialURL().then(url => {
    if (url) console.log('Launched from link:', url);
  });

  return () => subscription.remove();
}, []);
```

## Authentication Flow

The recommended pattern uses conditional rendering based on auth state rather than navigation:

```tsx
// src/navigation/RootNavigator.tsx
import { useAuthStore } from '../store/authStore';

const AuthStack = createNativeStackNavigator();
const AppStack = createNativeStackNavigator();

function AuthNavigator() {
  return (
    <AuthStack.Navigator screenOptions={{ headerShown: false }}>
      <AuthStack.Screen name="Welcome" component={WelcomeScreen} />
      <AuthStack.Screen name="Login" component={LoginScreen} />
      <AuthStack.Screen name="Register" component={RegisterScreen} />
    </AuthStack.Navigator>
  );
}

function AppNavigator() {
  return (
    <AppStack.Navigator screenOptions={{ headerShown: false }}>
      <AppStack.Screen name="Tabs" component={TabNavigator} />
      <AppStack.Screen name="PostDetail" component={PostDetailScreen} options={{ headerShown: true }} />
    </AppStack.Navigator>
  );
}

export function RootNavigator() {
  const { token, isLoading } = useAuthStore();

  if (isLoading) {
    return <SplashScreen />;
  }

  return token ? <AppNavigator /> : <AuthNavigator />;
}
```

:::caution[Navigation Ref for Imperative Navigation]
If you need to navigate from outside a React component (e.g., from an API interceptor or push notification handler), use a navigation ref:

```typescript
import { createNavigationContainerRef } from '@react-navigation/native';
export const navigationRef = createNavigationContainerRef<RootStackParamList>();

// In your NavigationContainer:
<NavigationContainer ref={navigationRef}>

// Then imperatively:
navigationRef.navigate('Login');
```
:::

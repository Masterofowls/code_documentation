---
title: Dark Mode with Tailwind CSS
description: 'Implement dark mode using the dark: variant, toggle between class and media strategies, store preferences, and apply dark-mode-aware colors and components.'
sidebar:
  order: 5
tags: [css, frontend]
---

import {
  Steps,
  Aside,
  Tabs,
  TabItem,
  Card,
  CardGrid,
} from '@astrojs/starlight/components';

Tailwind provides a `dark:` variant that activates when dark mode is in effect. You can choose between **class-based** and **media-query-based** dark mode strategies depending on your requirements.

## The `dark:` Variant

Any Tailwind utility can be prefixed with `dark:` to apply it in dark mode:

```html
<div class="bg-white text-gray-900 dark:bg-gray-900 dark:text-gray-100">
  Adapts to dark mode automatically
</div>

<button
  class="bg-blue-600 text-white hover:bg-blue-700
               dark:bg-blue-500 dark:hover:bg-blue-600"
>
  Dark-mode aware button
</button>

<p class="text-gray-600 dark:text-gray-400">Muted text in both modes</p>
```

## Dark Mode Strategies

### Media Query Strategy (default)

By default, Tailwind uses `prefers-color-scheme: dark` to detect the OS/browser preference:

```js
// tailwind.config.js
module.exports = {
  // darkMode defaults to 'media' — no config needed
  // darkMode: 'media', // explicit
};
```

```css
/* Generated CSS equivalent */
@media (prefers-color-scheme: dark) {
  .dark\:bg-gray-900 {
    background-color: rgb(17 24 39);
  }
}
```

**Pros:** Zero JavaScript required; respects user's OS preference automatically.
**Cons:** No user toggle; users can't override inside the app.

### Class Strategy

With `darkMode: 'class'`, dark mode activates when a `dark` class is present on the `<html>` element:

```js
// tailwind.config.js
module.exports = {
  darkMode: 'class',
  // ...
};
```

```html
<!-- Dark mode OFF -->
<html class="">
  <body class="bg-white dark:bg-gray-900">
    ...
  </body>
</html>

<!-- Dark mode ON -->
<html class="dark">
  <body class="bg-white dark:bg-gray-900">
    ...
  </body>
</html>
```

**Pros:** Full user control; supports in-app toggle; can override OS preference.
**Cons:** Requires JavaScript to manage the class.

:::tip[Which strategy to choose]
Use `'class'` for apps where users should be able to toggle dark mode (dashboards, editors, consumer apps). Use `'media'` for marketing sites or documentation where matching the OS setting is sufficient.
:::

## Implementing a Toggle with JavaScript

<Steps>
1. **Add the theme toggle script to your HTML `<head>`**

Place this before any stylesheets to prevent a flash of the wrong theme:

```html
<head>
  <script>
    // Immediately apply saved theme before page renders
    (function () {
      const saved = localStorage.getItem('theme');
      const prefersDark = window.matchMedia(
        '(prefers-color-scheme: dark)',
      ).matches;
      if (saved === 'dark' || (!saved && prefersDark)) {
        document.documentElement.classList.add('dark');
      }
    })();
  </script>
  <link rel="stylesheet" href="/styles.css" />
</head>
```

2. **Build the toggle button**

   ```html
   <button
     id="theme-toggle"
     class="p-2 rounded-lg text-gray-500 hover:text-gray-700 hover:bg-gray-100
            dark:text-gray-400 dark:hover:text-gray-200 dark:hover:bg-gray-700
            transition-colors"
     aria-label="Toggle dark mode"
   >
     <!-- Sun icon — shown in dark mode -->
     <svg
       id="icon-sun"
       class="hidden dark:block w-5 h-5"
       fill="none"
       stroke="currentColor"
       viewBox="0 0 24 24"
     >
       <path
         stroke-linecap="round"
         stroke-linejoin="round"
         stroke-width="2"
         d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364-6.364l-.707.707M6.343 17.657l-.707.707M17.657 17.657l-.707-.707M6.343 6.343l-.707-.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"
       />
     </svg>
     <!-- Moon icon — shown in light mode -->
     <svg
       id="icon-moon"
       class="block dark:hidden w-5 h-5"
       fill="none"
       stroke="currentColor"
       viewBox="0 0 24 24"
     >
       <path
         stroke-linecap="round"
         stroke-linejoin="round"
         stroke-width="2"
         d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"
       />
     </svg>
   </button>
   ```

3. **Wire up the JavaScript toggle logic**

   ```js
   // theme.js
   const toggle = document.getElementById('theme-toggle');
   const html = document.documentElement;

   toggle.addEventListener('click', () => {
     const isDark = html.classList.toggle('dark');
     localStorage.setItem('theme', isDark ? 'dark' : 'light');
   });
   ```

</Steps>

## React Toggle Implementation

```tsx
// hooks/useTheme.ts
import { useState, useEffect } from 'react';

type Theme = 'light' | 'dark' | 'system';

export function useTheme() {
  const [theme, setTheme] = useState<Theme>(() => {
    if (typeof window === 'undefined') return 'system';
    return (localStorage.getItem('theme') as Theme) ?? 'system';
  });

  useEffect(() => {
    const root = document.documentElement;
    const prefersDark = window.matchMedia(
      '(prefers-color-scheme: dark)',
    ).matches;

    const shouldBeDark =
      theme === 'dark' || (theme === 'system' && prefersDark);

    root.classList.toggle('dark', shouldBeDark);
    localStorage.setItem('theme', theme);
  }, [theme]);

  return { theme, setTheme };
}
```

```tsx
// components/ThemeToggle.tsx
import { useTheme } from '@/hooks/useTheme';

export function ThemeToggle() {
  const { theme, setTheme } = useTheme();

  return (
    <div className="flex items-center gap-1 p-1 bg-gray-100 dark:bg-gray-800 rounded-lg">
      {(['light', 'system', 'dark'] as const).map((option) => (
        <button
          key={option}
          onClick={() => setTheme(option)}
          className={`
            px-3 py-1.5 text-xs font-medium rounded-md capitalize transition-colors
            ${
              theme === option
                ? 'bg-white dark:bg-gray-700 text-gray-900 dark:text-white shadow-sm'
                : 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:hover:text-gray-200'
            }
          `}
        >
          {option}
        </button>
      ))}
    </div>
  );
}
```

## System Preference Detection

```js
// Listen for OS dark mode changes
const mediaQuery = window.matchMedia('(prefers-color-scheme: dark)');

function handleChange(event) {
  // Only update if user hasn't set an explicit preference
  if (!localStorage.getItem('theme')) {
    document.documentElement.classList.toggle('dark', event.matches);
  }
}

mediaQuery.addEventListener('change', handleChange);
```

## Next.js Dark Mode

With Next.js App Router, use `next-themes` for a complete, SSR-safe solution:

```bash
npm install next-themes
```

```tsx
// app/providers.tsx
'use client';

import { ThemeProvider } from 'next-themes';

export function Providers({ children }: { children: React.ReactNode }) {
  return (
    <ThemeProvider
      attribute="class" // Adds/removes 'dark' class on <html>
      defaultTheme="system" // Default to OS preference
      enableSystem // Enable system preference detection
      disableTransitionOnChange // Prevent flash on theme change
    >
      {children}
    </ThemeProvider>
  );
}
```

```tsx
// app/layout.tsx
import { Providers } from './providers';

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
```

```tsx
// components/ThemeToggle.tsx (Next.js / next-themes)
'use client';

import { useTheme } from 'next-themes';
import { useEffect, useState } from 'react';

export function ThemeToggle() {
  const { theme, setTheme } = useTheme();
  const [mounted, setMounted] = useState(false);

  // Avoid hydration mismatch — only render after mount
  useEffect(() => setMounted(true), []);
  if (!mounted) return null;

  return (
    <button
      onClick={() => setTheme(theme === 'dark' ? 'light' : 'dark')}
      className="p-2 rounded-lg bg-gray-100 hover:bg-gray-200
                 dark:bg-gray-800 dark:hover:bg-gray-700
                 text-gray-600 dark:text-gray-400 transition-colors"
    >
      {theme === 'dark' ? '☀️' : '🌙'}
    </button>
  );
}
```

## Dark Mode Color Palettes

Tailwind's color palette is designed to work well in both modes. Common patterns:

```html
<!-- Light background → Dark background -->
<div class="bg-white dark:bg-gray-950">
  <!-- Page background -->
  <div class="bg-gray-50 dark:bg-gray-900">
    <!-- Subtle background -->
    <div class="bg-gray-100 dark:bg-gray-800">
      <!-- Card / elevated surface -->
      <div class="bg-gray-200 dark:bg-gray-700">
        <!-- Hover state -->

        <!-- Text colors -->
        <p class="text-gray-900 dark:text-gray-100"><!-- Primary text --></p>
        <p class="text-gray-600 dark:text-gray-400"><!-- Secondary text --></p>
        <p class="text-gray-400 dark:text-gray-600">
          <!-- Muted / placeholder -->

          <!-- Borders -->
        </p>

        <div class="border-gray-200 dark:border-gray-700">
          <!-- Standard dividers -->
          <div class="border-gray-100 dark:border-gray-800">
            <!-- Subtle dividers -->
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
```

:::tip[Use slate, zinc, or neutral]
The `slate-*` and `zinc-*` palettes have a slight blue or warm tint that looks more polished than pure gray in dark interfaces. Try `bg-slate-900` for dark backgrounds.
:::

## Dark Mode with CSS Custom Properties

Combine Tailwind with CSS variables for consistent theming:

```css
/* globals.css */
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --color-background: 255 255 255; /* white */
    --color-foreground: 17 24 39; /* gray-900 */
    --color-surface: 249 250 251; /* gray-50 */
    --color-border: 229 231 235; /* gray-200 */
    --color-muted: 107 114 128; /* gray-500 */
    --color-accent: 109 40 217; /* violet-700 */
  }

  .dark {
    --color-background: 3 7 18; /* gray-950 */
    --color-foreground: 243 244 246; /* gray-100 */
    --color-surface: 17 24 39; /* gray-900 */
    --color-border: 55 65 81; /* gray-700 */
    --color-muted: 156 163 175; /* gray-400 */
    --color-accent: 139 92 246; /* violet-500 */
  }
}
```

```js
// tailwind.config.js — map CSS variables to theme colors
module.exports = {
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        background: 'rgb(var(--color-background) / <alpha-value>)',
        foreground: 'rgb(var(--color-foreground) / <alpha-value>)',
        surface: 'rgb(var(--color-surface) / <alpha-value>)',
        border: 'rgb(var(--color-border) / <alpha-value>)',
        muted: 'rgb(var(--color-muted) / <alpha-value>)',
        accent: 'rgb(var(--color-accent) / <alpha-value>)',
      },
    },
  },
};
```

```html
<!-- Using semantic color tokens -->
<div class="bg-background text-foreground">
  <div class="bg-surface border border-border rounded-xl p-6">
    <h2 class="text-foreground font-semibold">Card Title</h2>
    <p class="text-muted mt-1">Descriptive text</p>
    <button class="mt-4 bg-accent text-white px-4 py-2 rounded-lg">
      Action
    </button>
  </div>
</div>
```

## Dark Mode Images

```html
<!-- Invert a dark logo for light mode (so it shows in dark mode) -->
<img src="/logo-dark.svg" alt="Logo" class="h-8 dark:invert" />

<!-- Hide one image, show another based on mode -->
<img src="/diagram-light.png" alt="Diagram" class="block dark:hidden" />
<img src="/diagram-dark.png" alt="Diagram" class="hidden dark:block" />

<!-- Reduce brightness of photos in dark mode (less glaring) -->
<img
  src="/photo.jpg"
  alt="Photo"
  class="opacity-100 dark:opacity-75 dark:brightness-90 transition-all"
/>

<!-- Frosted glass card that adapts -->
<div
  class="backdrop-blur-md rounded-2xl border
            bg-white/70 border-gray-200/50
            dark:bg-gray-900/70 dark:border-gray-700/50
            p-6"
>
  Glass effect card
</div>
```

## Dark Mode Forms

```html
<form class="space-y-4">
  <div>
    <label
      class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1"
    >
      Email address
    </label>
    <input
      type="email"
      class="w-full px-3 py-2 rounded-lg border text-sm
             bg-white border-gray-300 text-gray-900 placeholder-gray-400
             dark:bg-gray-800 dark:border-gray-600 dark:text-gray-100 dark:placeholder-gray-500
             focus:outline-none focus:ring-2 focus:ring-violet-500 focus:border-transparent
             dark:focus:ring-violet-400
             transition-colors"
      placeholder="you@example.com"
    />
  </div>

  <div>
    <label
      class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1"
    >
      Message
    </label>
    <textarea
      rows="4"
      class="w-full px-3 py-2 rounded-lg border text-sm resize-none
             bg-white border-gray-300 text-gray-900 placeholder-gray-400
             dark:bg-gray-800 dark:border-gray-600 dark:text-gray-100 dark:placeholder-gray-500
             focus:outline-none focus:ring-2 focus:ring-violet-500 focus:border-transparent
             transition-colors"
      placeholder="Write your message..."
    ></textarea>
  </div>

  <button
    type="submit"
    class="w-full bg-violet-600 hover:bg-violet-700
           dark:bg-violet-500 dark:hover:bg-violet-600
           text-white font-medium py-2.5 px-4 rounded-lg
           transition-colors duration-150"
  >
    Send message
  </button>
</form>
```

:::caution[Input background in dark mode]
Browsers may auto-fill form fields with a light background that ignores your dark styles. Override this with:

```css
@layer base {
  input:-webkit-autofill,
  input:-webkit-autofill:hover,
  input:-webkit-autofill:focus {
    -webkit-box-shadow: 0 0 0px 1000px rgb(31 41 55) inset; /* gray-800 */
    -webkit-text-fill-color: rgb(243 244 246); /* gray-100 */
  }
}
```

:::
